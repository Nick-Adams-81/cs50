#include <stdio.h>

int main(void)
{
    // printf("meow\n");
    // printf("meow\n");
    // printf("meow\n");
    int counter = 5;
    while (counter > 0)
    {
        printf("meow\n");
        counter--;
    }
    return 0;
}